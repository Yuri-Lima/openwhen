{
  "name": "whenote-functions",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "@sendgrid/eventwebhook": "^8.0.0",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1",
    "stripe": "^17.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.3"
  }
}
