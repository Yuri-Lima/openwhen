# Changelog

Todas as mudanças notáveis neste projeto serão documentadas aqui.

O formato é baseado em [Keep a Changelog](https://keepachangelog.com/pt-BR/1.1.0/),
e este projeto adere ao [Semantic Versioning](https://semver.org/lang/pt-BR/) onde aplicável.

---

## [Unreleased]

### Added

- **Hash criptográfico em audit logs (`delete_account.ts`, `request_deletion.ts`, `export_user_data.ts`, `scheduled_deletion.ts`):** substituída `simpleHash()` (djb2, 32-bit, sem salt, colisões triviais) por `hashUid()` — HMAC-SHA-256 com salt estático (`whenote-audit-log-v1-2026`), output truncado a 16 hex chars (64 bits). Resistente a colisões e não-reversível. Aplicado em todos os 4 ficheiros de backend que pseudonimizam UIDs em `deletionAuditLogs` e `privacyRequestLogs`. `simpleHash()` marcada como `@deprecated` mas mantida temporariamente para referência. `scheduled_deletion.ts` corrigido: usava `uid.substring(0, 8) + "..."` (PII parcial em texto claro), agora usa `hashUid(uid)`.

- **Audit trail de privacidade completo (`privacy_log_service.dart`, `firestore.rules`, `settings_screen.dart`):** expandido o `PrivacyLogService` com 3 novos métodos: `logDeletionRequest()` (regista pedido de exclusão com modo e provider), `logDeletionCancellation()` (regista cancelamento de exclusão pendente), `logReauthentication()` (regista tentativa de re-auth social — Apple/Google). Todos os métodos seguem o padrão fire-and-forget com try/catch (best-effort). Chamadas injetadas em `settings_screen.dart`: `_executeAccountDeletion()` (success + failure), `_cancelDeletion()` (success + failure), `_handleSocialReauth()` (success + failure), `_handleConfirm()` / password re-auth (success + failure). Firestore rules atualizadas: whitelist de `type` expandida de `['export']` para `['export', 'complete_export', 'deletion_request', 'deletion_cancellation', 'reauthentication']` — sem esta correção, todas as novas chamadas eram silenciosamente rejeitadas por `permission-denied`. Review fix: `logDeletionCancellation` agora inclui `'metadata': {}` (campo obrigatório pelo schema `keys().hasOnly()` das rules).

- **Export completo: cápsulas recebidas + coordenadas GPS (`complete_export_service.dart`):** corrigido gap de portabilidade (GDPR Art. 20): o export só incluía cápsulas enviadas (`senderUid == uid`), faltavam as recebidas/participadas. Adicionado `_fetchCapsules()` com query `participantUids array-contains uid` + deduplicação por `byId` map, espelhando o padrão de `_fetchLetters()`. Fotos de cápsulas recebidas agora também são incluídas no ZIP. Corrigido P2 da review: coordenadas GPS agora exportadas como `{latitude, longitude}` em vez de apenas `hasLocation: true` — o utilizador forneceu os dados, portabilidade exige incluí-los. Adicionado helper `_geoPointToMap()` com suporte a `GeoPoint` e Map raw.

- **Re-autenticação social para eliminação de conta (`account_deletion_service.dart`, `settings_screen.dart`):** utilizadores que fizeram login via Apple ou Google não conseguiam eliminar a conta porque `reauthenticateWithPassword()` falhava — não têm password. Adicionado: `currentAuthProvider()` que deteta o provider (prioriza social sobre password para utilizadores com múltiplos providers), `_reauthenticateWithGoogle()` via `GoogleSignIn().signIn()` + `GoogleAuthProvider.credential`, `_reauthenticateWithApple()` via `SignInWithApple` com nonce SHA-256 + `OAuthProvider('apple.com').credential`. O `_DeleteAccountSheet` agora deteta o provider e mostra: campo de password (email/password) OU botão "Verify with Apple/Google" (social). A re-auth social acontece dentro do sheet com feedback visual (spinner, estado "verified", erro). 4 chaves i18n novas em 4 idiomas: `settingsDeleteReauthLabel`, `settingsDeleteReauthApple`, `settingsDeleteReauthGoogle`, `settingsDeleteReauthSuccess`. Review: corrigido missing `await` no `onConfirm`, hardcoded colors dark mode, e provider iteration order.

- **Anonimização automática de feedback — 1 ano (`functions/src/anonymize_old_feedback.ts`):** Cloud Function scheduled (`onSchedule`, diariamente às 05:00 UTC) que cumpre o compromisso da secção 9 da Política de Privacidade: "Product feedback: anonymized after 1 year." Utiliza query de janela temporal (24h): feedback com `createdAt` entre 366 e 365 dias atrás — custo constante independente do volume acumulado. Backfill com `select("anonymizedAt")` + `limit(450)` para feedback anterior ao deploy. Campos PII removidos via `FieldValue.delete()`: `uid`, `message`. Campos estatísticos mantidos: `type`, `createdAt`, `appLocale`, `platform`. Campo `anonymizedAt` adicionado como timestamp de auditoria. Batch writes com limite de 450 operações. Timeout: 300s, retry: 1. Exportada no `index.ts`.

- **Purge automático de logs de moderação — 2 anos (`functions/src/purge_old_moderation_logs.ts`):** Cloud Function scheduled (`onSchedule`, diariamente às 04:30 UTC) que cumpre o compromisso da secção 9 da Política de Privacidade: "Moderation logs: 2 years." Elimina completamente (não anonimiza) documentos de `moderationIncidents` e `moderationReviews` com `createdAt` anterior a 730 dias. Paginação interna: query com `limit(450)` em loop até esgotar documentos elegíveis, evitando OOM em volumes grandes. PII eliminado: `callerUid`, `message` (incidents); `authorUid`, `authorDisplayName`, `text`, `resolvedByUid`, `adminFeedback` (reviews). Timeout: 300s, retry: 1. Exportada no `index.ts`. Sem composite index necessário.

- **Anonimização automática de denúncias — 90 dias (`functions/src/anonymize_resolved_reports.ts`):** Cloud Function scheduled (`onSchedule`, diariamente às 04:00 UTC) que cumpre o compromisso da secção 9 da Política de Privacidade: "Content reports: anonymized 90 days after resolution." Query composta: `status` in [resolved, dismissed] + `resolvedAt` ≤ now − 90 dias + `anonymizedAt` == null. Campos PII removidos via `FieldValue.delete()`: `reporterUid`, `detail`, `resolvedByUid`. Campos estatísticos mantidos: `targetType`, `targetId`, `letterId`, `reason`, `status`, `createdAt`, `resolvedAt`. Campo `anonymizedAt` adicionado como timestamp de auditoria. Batch writes com limite de 450 operações por batch para respeitar o limite Firestore de 500. Timeout: 300s, retry: 1. Exportada no `index.ts`.

- **Exportação completa de dados — GDPR Art. 20 / LGPD Art. 18 (`complete_export_service.dart`, `settings_screen.dart`, `privacy_log_service.dart`):** novo serviço `lib/core/export/complete_export_service.dart` que exporta todos os dados do utilizador num ficheiro ZIP: perfil, cartas (sent + received, deduplicadas), cápsulas (+ fotos), comentários, likes, follows (followers + following), badges — todos em JSON (`whenote-sar-v1` format) + media (voz .m4a, manuscrito .jpg, fotos de cápsulas .jpg). Queries Firestore paginadas (500 docs/batch) com `startAfterDocument` para evitar OOM em contas grandes. Sanitização de campos internos (UIDs alheios, stripeCustomerId, searchTokens, etc.) com conversão de Timestamps para ISO-8601. URLs de media validadas contra allowlists de Firebase Storage (proteção SSRF). UI no Settings atualizada: restrição Pro removida — export gratuito para todos os utilizadores; novo `_CompleteExportButton` StatefulWidget com `LinearProgressIndicator` e estágios; partilha via `share_plus`. `PrivacyLogService.logCompleteExport()` adicionado para audit trail com metadata detalhada (contagens por tipo). Chaves i18n atualizadas/adicionadas em 4 idiomas: `settingsExportData`, `settingsExportDataSubtitle`, `settingsExportCompleteSuccess` (novas); `settingsExportTitle`, `settingsExportBody`, `settingsExportZipSubtitle` (atualizadas).

- **Consentimento de analytics EU/EEA/UK — ePrivacy / UK PECR (`consent_constants.dart`, `analytics_consent_provider.dart`, `analytics_consent_banner.dart`, `analytics_service.dart`, `age_verification.dart`, `app_user.dart`, `main.dart`, `settings_screen.dart`):** Firebase Analytics desativado por defeito (`setAnalyticsCollectionEnabled(false)`) no arranque do app, antes de qualquer evento. Novo módulo `lib/core/consent/` com: enum `AnalyticsConsentStatus` (pending/granted/denied), provider Riverpod `analyticsConsentProvider` que lê/grava SharedPreferences + sync best-effort para Firestore (`analyticsConsent`, `analyticsConsentDate`), e widget `AnalyticsConsentOverlay` — banner slide-up não-modal mostrado apenas para utilizadores EU/EEA/UK (detecção via `isEuEeaUkJurisdiction()` reutilizando o set de 31 países de `age_verification.dart`). Utilizadores fora da EU recebem auto-grant sem banner. Guard `if (!_enabled) return` adicionado a todos os 18+ métodos de `AnalyticsService`. Toggle "Usage analytics" (SwitchListTile) adicionado à secção Privacidade do Settings, visível apenas para EU/EEA/UK. 6 chaves ARB novas em en/pt/pt-BR/es. Campos `analyticsConsent` e `analyticsConsentDate` adicionados ao modelo `AppUser` (nullable para retrocompatibilidade).

- **Verificação de idade por jurisdição — GDPR Art. 8 / COPPA (`age_verification.dart`, `register_screen.dart`, `login_screen.dart`, `auth_repository.dart`, `auth_provider.dart`, `app_user.dart`):** substituído o antigo checkbox de confirmação de idade por um date picker de data de nascimento no registro (email/password) e um age gate modal no login social (Google/Apple). Novo utilitário `lib/core/utils/age_verification.dart` com função `validateAge(DateTime dateOfBirth)` que determina a idade mínima com base na jurisdição do dispositivo (`Platform.localeName`): 27 países EU/EEA/UK com idades entre 13–16 conforme GDPR Art. 8; restantes defaultam a 16 (EU) ou 13 (COPPA). Campo `dateOfBirth` (`DateTime?`, nullable para retrocompatibilidade) adicionado ao modelo `AppUser`; gravado no Firestore como `Timestamp` em `users/{uid}` durante registro e primeiro login social (`isNewUser`). 3 chaves ARB novas (`registerDateOfBirthLabel`, `registerDateOfBirthHint`, `registerAgeUnder`) em en/pt/pt-BR/es; chave morta `registerConfirmAge` removida de todos os ficheiros de localização.

- **Aviso emocional antes de abrir carta (`letter_emotional_primer_screen.dart`, `letter_primer_prefs.dart`, `open_with_proximity.dart`):** tela exibida uma única vez por dispositivo, antes da animação de abertura da carta. Mostra "Essa carta pode ser emocional. Abra quando estiver pronto." com botões "Abrir agora" e "Ver depois". Ambos os botões gravam o flag `letter_emotional_primer_seen` via `SharedPreferences` (fail-open: erros de storage são silenciados). A verificação é hoisted para o topo de `openLetterWithProximityGate`, antes do proximity check, para evitar chamada desnecessária de geolocalização quando o utilizador escolhe adiar. `LetterOpeningScreen`, `CapsuleOpeningScreen` e toda a camada de backend ficam inalterados. 4 chaves ARB novas: `letterEmotionalPrimerTitle`, `letterEmotionalPrimerBody`, `letterEmotionalPrimerOpenNow`, `letterEmotionalPrimerViewLater` (en/pt-BR/pt/es).

- **Sign in with Apple (`auth_service.dart`, `auth_repository.dart`, `login_screen.dart`):** login social via Apple usando `OAuthProvider('apple.com')` com nonce SHA-256 (`_generateNonce` + `_sha256ofString`). Scopes: `email` + `fullName`. Entitlements `com.apple.developer.applesignin` adicionados a `Runner.entitlements` e `RunnerRelease.entitlements`. Provedor Apple ativado no Firebase Console. Na primeira autenticação (`isNewUser`), cria documento Firestore com o mesmo schema do registo por email (searchTokens, subscription tier, counters, etc.). `displayName` persistido no Firebase Auth profile quando Apple o fornece (só no primeiro login). Botão visível apenas no iOS (`Platform.isIOS`). Cancelamento pelo utilizador tratado silenciosamente via `SignInWithAppleAuthorizationException`. Packages: `sign_in_with_apple: ^6.1.4`, `crypto: ^3.0.6`.

- **Google Sign-In (`auth_service.dart`, `auth_repository.dart`, `login_screen.dart`):** login social via Google usando `GoogleSignIn().signIn()` + `GoogleAuthProvider.credential`. `REVERSED_CLIENT_ID` configurado como URL scheme no `Info.plist` (`CFBundleURLTypes`) para OAuth redirect no iOS. Na primeira autenticação, cria documento Firestore com schema completo (inclui `photoUrl` do perfil Google). `signOut()` atualizado para também chamar `GoogleSignIn().signOut()` (permite troca de conta). Botão visível em todas as plataformas. Cancelamento tratado via string `google-sign-in-cancelled`. Package: `google_sign_in: ^6.2.2`.

- **Social sign-in ativado no login:** `kSocialSignInEnabled` alterado de `false` para `true`. `_buildSocialButtons()` reescrito como `Column` com Apple (iOS only) + Google (all platforms), cada um com `GestureDetector` e guard `_isLoading`. Analytics: `AnalyticsService.logLogin(method: 'apple'|'google')` — parâmetro `method` tornado opcional com default `'email'`.

### Fixed

- **Cursor preso + menu copiar/colar ausente no campo de mensagem da carta (`write_letter_screen.dart`):** dois problemas distintos com a mesma raiz. (1) O `GestureDetector(onTap: setState(_showResults=false))` envolvia o `SingleChildScrollView` inteiro — o gesto de toque capturava o evento antes do `TextField`, impedindo o reposicionamento do cursor ao tocar no meio do texto e suprimindo o menu de contexto nativo (Copiar / Colar) acionado por toque longo. (2) No estado colapsado, o campo de mensagem era um widget `Text` estático (pré-visualização read-only), sem `TextEditingController` ativo — não havia cursor real nem integração com o clipboard do sistema. **Correção:** campo de mensagem unificado num único `TextField` com `minLines: 1` / `maxLines: _messageExpanded ? 8 : 1`, `enableInteractiveSelection: true`; `FocusNode` listener `_onMessageFocusChanged()` regista-se em `initState` e remove-se em `dispose` — ao receber foco com campo colapsado, expande automaticamente e faz `Scrollable.ensureVisible`; `GestureDetector` global removido e substituído por `TapRegion` isolado apenas na zona de busca de destinatário + lista de resultados, com `onTapOutside` adiado via `addPostFrameCallback` para não cancelar gestos longos noutros campos; `isDense` removido (no-op com `contentPadding` explícito); `contentPadding` diferenciado por estado (`fromLTRB(16,0,16,16)` expandido; `symmetric(h:16,v:12)` colapsado). Bloqueador **B2** do MVP fechado.

- **Aviso Camada 1 (palavras inadequadas) alinhado em cartas e cápsulas:** lista lexical e SnackBar passam a usar a mesma chave ARB que comentários (`commentsModerationWarning`, já em en/pt/pt-BR/es). Novo `lib/core/moderation/banned_lexical_words.dart` com `textContainsBannedLexicalWord`; `comments_screen.dart` deixa de duplicar mapas. Em `write_letter_screen.dart` e `create_capsule_screen.dart`, verificação antes da moderação por IA. Ver [`MODERATION.md`](MODERATION.md).

### Changed

- **Menção ao Global Privacy Control (GPC) removida da Política de Privacidade:** frase "We honor the Global Privacy Control (GPC) signal as a valid opt-out request under CCPA" removida da secção 15 (Tracking Technologies) nos 4 idiomas (en/pt/pt-BR/es). GPC é um header HTTP de browser (`Sec-GPC: 1`) — não aplicável a apps nativos móveis. A promessa era inimplementável. O opt-out de analytics para utilizadores CCPA fica coberto pelo toggle nas Settings. Menção será re-adicionada quando existir versão web com implementação real.

- **SMTP customizado migrado de SendGrid para Google Workspace SMTP Relay (2026-04-26):** o envio de emails de autenticação Firebase (verificação de email, reset de password, email change) foi migrado de SendGrid (`smtp.sendgrid.net`) para o SMTP Relay do Google Workspace (`smtp-relay.gmail.com`). Configuração feita em duas etapas: (1) Google Admin Console → Apps → Google Workspace → Gmail → Routing → SMTP relay service: allowed senders "Any addresses", "Require SMTP Authentication" ✅, "Require TLS encryption" ✅; (2) Firebase Console → Authentication → Templates → SMTP settings: host `smtp-relay.gmail.com`, porta 587, STARTTLS, username `yurilima@whenote.com`, password = App Password gerada na conta Workspace. Sender address alterado para `noreply@whenote.com`. Pré-requisito: 2-Step Verification ativada na conta Workspace. Motivação: eliminar dependência do SendGrid e simplificar stack usando serviço já incluído no Google Workspace.

### Added

- **Página de suporte (FAQ) — Firebase Hosting:** `hosting/public/support.html` com 7 perguntas frequentes, alternância de idioma EN/PT/ES, dark mode, mesmo design de `privacy.html` e `terms.html`. URL: `https://whenote.app/support`. Email de contacto: `support@whenote.com`. Rewrites adicionados em `firebase.json` (`/support` e `/support/` → `/support.html`). **Deploy Hosting concluído (2026-05-01):** `firebase deploy --only hosting`.

- **Moderação de mídia (imagens + áudio) via Cloud Function Storage trigger:** nova Cloud Function `moderateUploadedFile` (`onObjectFinalized`) que intercepta uploads em `avatars/`, `capsules/photos/`, `handwritten/` e `voiceLetters/`. Imagens são analisadas via OpenAI `omni-moderation-latest` (gratuito, 13 categorias); áudio é transcrito via Whisper API e o texto moderado. Se flagged: ficheiro deletado do Storage, referência Firestore limpa (photoUrl, photos array, handwrittenImageUrl, voiceUrl), incidente registado, notificação enviada ao utilizador. Fail-open em erros de provider. Novo `moderate_storage.ts`, métodos `moderateImageWithOpenAI()` e `transcribeAudioWithWhisper()` em `openai_adapter.ts`, `copyMediaRemoved()` em `notification_copy.ts` (3 idiomas), 6 strings l10n em 4 ARBs. Ver [`MODERATION.md`](MODERATION.md).

### Fixed

- **Bug avatar path inconsistency:** `ProfileScreen` tinha método morto `_pickAndUploadAvatar()` que usava path `avatars/$uid/avatar.jpg` (incompatível com Storage rules `avatars/{uid}.jpg`). Método removido — upload usa exclusivamente `AvatarUploadHelper` → `AvatarService` com path correto. Imports não usados (`dart:typed_data`, `firebase_storage`, `image_picker`) também removidos.

- **Página de ação customizada para emails de auth (Firebase Hosting):** `hosting/public/auth/action.html` — página dark theme com branding Whenote (envelope com selo OW) para verificação de email e reset de password. Usa Firebase JS SDK v10.12.0, projeto `whenote-923f5`. Modos: `verifyEmail` (aplica action code, mostra sucesso/erro), `resetPassword` (formulário nova password + confirmação, chama `confirmPasswordReset`). Headers de segurança (`no-cache`, `nosniff`, `DENY`) em `firebase.json` para `auth/**`. Cores do app: bg `#1A1714`, card `#252018`, accent `#E74C3C`, text `#F5F0EB`. Templates HTML de email (`hosting/email-templates/verify-email.html`, `reset-password.html`) com layout table-based para compatibilidade com clientes de email, placeholder `{{ACTION_URL}}`. Guia completo: [`EMAIL_SETUP.md`](EMAIL_SETUP.md).
- **SMTP customizado (SendGrid) para emails de autenticação Firebase:** domínio `whenote.app` autenticado no SendGrid (`em2352.whenote.app`, SPF/DKIM via CNAMEs). SMTP configurado no Firebase Console: `smtp.sendgrid.net`, porta 587, STARTTLS, username `apikey`. Action URL global alterada de `https://whenote-923f5.firebaseapp.com/__/auth/action` para `https://whenote.app/auth/action.html`. Sender name alterado para "Whenote" nos 3 templates (Email address verification, Password reset, Email address change). Domínio customizado do remetente (`noreply@whenote.app` em vez de `noreply@whenote-923f5.firebaseapp.com`) em processo — 4 registros DNS (2 TXT + 2 CNAME DKIM) pendentes de adição no Cloudflare.

### Fixed

- **Redirect pós-registro — utilizador não era redirecionado ao app:** após `createUserWithEmailAndPassword`, o Firebase faz auto-login mas a `RegisterScreen` (empilhada via `Navigator.pushNamed('/register')`) permanecia acima do `AuthWrapper` na stack de navegação. Adicionado `Navigator.of(context).popUntil((route) => route.isFirst)` após o snackbar de sucesso — a `RegisterScreen` é removida e o `AuthWrapper` detecta o utilizador logado, mostrando a `HomeScreen`. Ver [`register_screen.dart`](../lib/features/auth/presentation/screens/register_screen.dart).

- **Validação de email externo + notificação de bounce ao remetente:** fluxo completo para cartas enviadas a emails externos sem conta. **Cliente:** validação com regex reutilizável em `lib/core/utils/validators.dart` (substitui `contains('@')`); enum `InviteEmailStatus` em `letter.dart` com `fromString` seguro; banner vermelho no detalhe da carta (sender) com botão de reenvio; dialog de reenvio que chama `resendExternalInviteEmail`; tela de notificações expandida para tipo `email_bounce` com ícone diferenciado; mensagens ARB melhoradas + 6 novas keys em 4 idiomas. **Servidor:** Cloud Function `onSendGridWebhook` (webhook HTTP) para eventos SendGrid (bounce/dropped/delivered/deferred) com verificação de assinatura ECDSA (`rawBody`), `defineSecret()` para secrets, batch chunking (≤250), idempotência por estado, notificação in-app + push FCM localizado (`preferredLanguage`), logging estruturado com email mascarado; `custom_args` (letterId + senderUid) no envio SendGrid; estado `inviteEmailStatus: "sent"/"send_failed"` em `onLetterCreatedSendExternalInviteEmail`; callable `resendExternalInviteEmail` com rate limiting (5 min cooldown via `lastResendAt`); migração de `SENDGRID_API_KEY` para `defineSecret()`. **Segurança:** Firestore rules protegem campos imutáveis (`senderUid`, `receiverUid`, `createdAt`, `inviteEmailStatus*`, `inviteEmailSentAt`, `receiverEmailNormalized`, `deliveryMode`, `lastResendAt`); delete restrito ao sender. **Testes:** `test/core/validators_test.dart` (7 cenários, todos passando). Plano: [`EMAIL_VALIDATION_PLAN.md`](EMAIL_VALIDATION_PLAN.md).

- **Deploy e configuração do fluxo de email bounce (SendGrid → Firebase):** Event Webhook configurado no painel SendGrid (eventos: `delivered`, `bounced`, `dropped`, `deferred`) com Signature Verification (ECDSA) habilitada. Secrets `SENDGRID_API_KEY` e `SENDGRID_WEBHOOK_VERIFICATION_KEY` migrados para Firebase Secret Manager (`defineSecret()`); `SENDGRID_API_KEY` removido de `functions/.env` para resolver conflito de variável secreta vs. não-secreta no Cloud Run. Nova function `resendExternalInviteEmail` criada em produção.
- **Sincronização de `preferredLanguage` com Firestore:** campo `preferredLanguage` (código 2 letras: `pt`, `en`, `es`) adicionado ao documento do utilizador na criação de conta (`ensure_user_firestore_profile.dart`, `auth_repository.dart`) e sincronizado em tempo real quando o utilizador muda o idioma no Settings (`locale_provider.dart` → `_syncToFirestore`, best-effort). Webhook `onSendGridWebhook` atualizado com fallback: tenta `preferredLanguage`, depois `language` (extrai 2 primeiros chars de `pt-BR`), e por último `en` — compatível com utilizadores existentes.

### Changed

- **Envio de carta — botão OK do email removido (UX simplificado):** o campo de email para destinatários sem conta tinha um botão "OK" ao lado que o utilizador precisava clicar antes de enviar a carta. Removido o botão; agora o email é validado e resolvido automaticamente ao clicar "Enviar carta" — se `_receiverName` está vazio e o campo de email tem texto, `_saveLetter()` valida o formato e define o destinatário inline, eliminando um passo de interação desnecessário. Método `_selectByEmail()` removido. Ver [`write_letter_screen.dart`](../lib/features/letters/presentation/screens/write_letter_screen.dart).
- **Toggle de privacidade na escrita de carta — UX invertido:** o switch de privacidade em `WriteLetterScreen` usava `_allowPublish = false` (switch visualmente **desligado** por padrão), o que passava a falsa impressão de que a carta não estava protegida. Invertido para `_isPrivate = true` (switch **ligado/ativo** por padrão com ícone de cadeado e cor accent), deixando claro que a carta é privada. Comportamento no Firestore inalterado (`publishAfterReview: !_isPrivate`). Ver [`write_letter_screen.dart`](../lib/features/letters/presentation/screens/write_letter_screen.dart).

### Added

- **Política de Privacidade completa (LGPD + GDPR + CCPA/CPRA + COPPA):** reescrita total com 17 seções (anteriormente 8). Cobre: definições legais, inventário real de todos os dados coletados (incl. GPS preciso, voz, câmera, moderação IA, analytics, billing), bases legais mapeadas por framework (LGPD Art. 7 + GDPR Art. 6), decisões automatizadas (OpenAI/Art. 22 GDPR/Art. 20 LGPD), compartilhamento detalhado por terceiro (Firebase, OpenAI, SendGrid, Stripe, Google Fonts), transferências internacionais (SCCs/Schrems II/LGPD Art. 33), retenção com períodos específicos por categoria, direitos separados por jurisdição (LGPD/GDPR/CCPA-CPRA com prazos e autoridades), exclusão de conta (dois modos), portabilidade (ZIP/JSON), privacidade infantil (COPPA 13+), segurança (TLS 1.3, App Check, breach 72h GDPR), tracking (Firebase Analytics/Crashlytics, GPC signal), alterações (aviso 15 dias), contatos (DPO, ANPD, supervisory authority, CA AG). Implementada em 3 idiomas (EN, PT-BR, ES) em [`LEGAL.md`](LEGAL.md) + `legal_screen.dart`.
- **Termos de Uso — cláusula de encerramento de serviço:** nova Seção 7 "Descontinuação do Serviço e Garantia de Entrega" — aviso 90 dias, exportação de dados, entrega de cartas pendentes, Fundo de Continuidade (reserva 2 anos) em [`LEGAL.md`](LEGAL.md). Em 3 idiomas.
- **Aviso de cartas pendentes na exclusão de conta:** banner de aviso amarelo no bottom sheet de `_DeleteAccountSheet` em `settings_screen.dart` explicando impacto em cartas locked enviadas/recebidas para cada modo (Excluir Tudo vs Anonimizar). String `settingsDeletePendingLettersWarning` em 3 idiomas.
- **Plano de contingência de encerramento:** [`LEGAL.md`](LEGAL.md) (secção 1) com cronograma dia-a-dia (90 dias), tratamento de cartas pendentes (4 cenários), Fundo de Continuidade (meta, alimentação, transparência), aspectos legais LGPD/GDPR/CCPA, responsabilidades e checklist de testes.
- **Páginas web públicas (sem login):** `hosting/public/privacy.html` e `hosting/public/terms.html` com alternância de idioma EN/PT/ES, suporte dark mode, links cruzados. Acessíveis via Firebase Hosting sem necessidade de conta.

- **Deleção de conta completa (GDPR/LGPD):** Cloud Function `deleteUserAccount` com 15 etapas atômicas — cancela Stripe, processa cartas/cápsulas (delete ou anonimize), remove comments/likes/follows/blocks/reports/feedback, limpa subcoleções (badges, notificações), apaga Storage (avatar, voz, handwritten, fotos de cápsulas), deleta Auth, grava audit log sem PII. Dois modos: "Excluir Tudo" e "Anonimizar cartas" — escolha do usuário no modal.
- **Re-autenticação antes de deletar conta:** `AccountDeletionService` com `reauthenticateWithPassword()` + chamada à Cloud Function via `CallableQueue`. UI com loading overlay e tratamento de erros (senha errada, timeout, etc.).
- **Checkbox idade 13+ (COPPA):** tela de registro exige confirmação "Tenho 13 anos ou mais" e aceite de Termos de Uso + Política de Privacidade via checkboxes explícitos. Botão de registro desabilitado até ambos marcados.
- **Política de retenção de dados:** documento `planning/DATA_RETENTION_POLICY.md` com mapeamento completo de dados (15 coleções Firestore + 4 paths Storage), requisitos GDPR/LGPD/COPPA, fluxo de deleção, checkboxes necessários, política de retenção por categoria, e plano de implementação priorizado.
- **Coleções `follows`, `blocks`, `deletionAuditLogs`:** adicionadas a `FirestoreCollections` constants.
- **16 novas strings l10n** (en/pt/es): modal de deleção (escolha, senha, confirmação, loading, erros) e checkboxes de registro.

### Fixed

- **Feed — botões de busca e notificações não funcionavam:** `_iconBtn` no header do feed era puramente visual (sem `onTap` / `GestureDetector`). Refactorado para aceitar `onTap` e `tooltip` opcionais; quando interativo, envolve com `Semantics` (a11y), `Tooltip`, `SizedBox(48×48)` (alvo Material 3) e `Material` + `InkWell` (ripple). Lupa navega via `pushNamed('/search')` (preserva `DeferredSearchPage`); sino navega para `ModerationNotificationsScreen` (mesmo padrão de Settings). Ver [`feed_screen.dart`](../lib/features/feed/presentation/screens/feed_screen.dart).
- **Busca de utilizadores — erros silenciosos:** os dois `catch (_)` em [`UserSearchService.search`](../lib/core/user_search/user_search_service.dart) engoliam exceções de Firestore (índice em falta, permissão, rede). Substituídos por `catch (e)` com `debugPrint` protegido por `kDebugMode` — sem overhead em release, diagnóstico imediato em debug.
- **Enviar carta — Firestore `permission-denied`:** regras `badgeUnlocks` exigiam `unlockedAt is timestamp` enquanto o cliente usa `FieldValue.serverTimestamp()` (sentinela); alinhado a `unlockedAt == request.time` **ou** `is timestamp`, e comparações de billing em `userBillingFieldsUnchanged` / `userBillingOnlyFreeTierMigration` com `.get('campo', null)` para documentos sem `subscriptionTier`/Stripe. **Cliente:** envio numa única **`runTransaction`** ([`letter_send_service.dart`](../lib/features/letters/data/letter_send_service.dart)) — carta + contador + badges tudo-ou-nada; mensagens de erro por fase (`LetterSendStep`) e código Firebase visível no SnackBar. Ver [`planning/TROUBLESHOOTING.md`](TROUBLESHOOTING.md).
- **Admin — ecrã Moderação:** `setState` durante `initState` ao disparar loaders — app podia fechar ao abrir; loaders adiados com `addPostFrameCallback` ([`admin_moderation_screen.dart`](../lib/features/admin/presentation/admin_moderation_screen.dart)). **Regressão a evitar:** não voltar a disparar os cinco callables HTTPS admin **em paralelo** no primeiro paint — em dispositivo iOS isso correlacionou com `SIGABRT`; os loaders passam a correr **em série** (`_loadAllAdminData`). Documentado em [`planning/TROUBLESHOOTING.md`](TROUBLESHOOTING.md) §2; `SKIP_AI_MODERATION` só afecta comentários, não o admin.
- **Busca de utilizadores (escala / Firestore)** — **Problema (até ~abril/2026):** a tela **Buscar**, o convite em **cápsulas coletivas** e a busca de destinatário em **Escrever carta** usavam `collection('users').get()`, carregando **todos** os perfis na memória e filtrando no cliente (incluindo por email). Com centenas ou milhares de utilizadores, custo Firestore, RAM e UI degradavam de forma inaceitável. **Correção:** [`UserSearchService`](../lib/core/user_search/user_search_service.dart) com queries **limitadas** (até 30 documentos): prefixo indexado em `username` + `array-contains` em `searchTokens`; campo `searchTokens` gerado por [`buildUserSearchTokens`](../lib/core/user_search/user_search_tokens.dart) no **registo** e ao **guardar perfil**; mínimo 2 caracteres na descoberta; estado Seguir/Seguindo obtido em **batch** ([`user_search_follows.dart`](../lib/core/user_search/user_search_follows.dart)). A API de busca expõe apenas `uid`, `@username`, nome público e `photoUrl` (sem email na busca). Ver [`ARCHITECTURE.md`](ARCHITECTURE.md) (secção “Busca de utilizadores”).

### Docs

- **App Store Connect (iOS 1.0) — metadata configurado:** descrição multi-parágrafo (3 128 chars), keywords (85/100 chars), texto promocional, subtítulo "Letters to the future", URLs de suporte (`whenote.app/support`) e marketing (`whenote.app`), copyright 2026 Whenote, categorias Social Networking + Lifestyle, age ratings 4+ (UGC = YES, restante NONE/NO), pricing Free em 175 países, contacto de revisão (Yuri Lima). Pendente para submissão: screenshots (mín. 3 iPhone 6.5").
- **Troubleshooting:** [`TROUBLESHOOTING.md`](TROUBLESHOOTING.md) — envio de carta (`permission-denied`), regras/deploy; **§2** ecrã admin (post-frame + callables **sequenciais**, não paralelos; `SKIP_AI_MODERATION` só comentários); **§4** bundle/FCM com texto genérico alinhado ao `BUNDLE_ID` do plist; nota iOS; referências cruzadas em [`PRODUCTION.md`](PRODUCTION.md), [`ARCHITECTURE.md`](ARCHITECTURE.md), [`README.md`](../README.md) / [`README.pt-BR.md`](../README.pt-BR.md).
- **Busca de utilizadores (escala):** problema histórico e correção descritos na secção **Fixed** acima; documentação actualizada em [`ARCHITECTURE.md`](ARCHITECTURE.md), [`PRODUCTION.md`](PRODUCTION.md), [`MVP_CHECKLIST.md`](MVP_CHECKLIST.md), [`README.md`](../README.md), [`README.pt-BR.md`](../README.pt-BR.md), [`ROADMAP.md`](ROADMAP.md) e [`CLAUDE.md`](../CLAUDE.md).
- **SDK e Firebase:** [`README.md`](../README.md) / [`README.pt-BR.md`](../README.pt-BR.md) — badges e pré-requisitos alinhados a Flutter **3.41.5** e Dart **3.11.3** (stable atual); secção Firebase atualizada: `firebase_options.dart`, `google-services.json` e `GoogleService-Info.plist` estão **versionados** para `whenote-923f5`. [`planning/PRODUCTION.md`](PRODUCTION.md) secção 1 — título e texto coerentes com ficheiros no repositório; âncoras da checklist atualizadas. [`planning/ARCHITECTURE.md`](ARCHITECTURE.md) — comentário na árvore `lib/` para `firebase_options.dart`. [`FLUTTER_COMMANDS.md`](../FLUTTER_COMMANDS.md) — linha de baseline de SDK com ponte para o README.
- **Stripe / negócio:** [`BUSINESS.md`](BUSINESS.md) — secção “Texto para onboarding Stripe (KYC)” com respostas curtas/longas (EN) e referência PT para formulários; alinhado a assinaturas (Amanhã / Brisa / Horizonte), serviços digitais e roadmap (Gift, pay-per-feature). [`PRODUCTION.md`](PRODUCTION.md) — ponte na secção Cloud Functions / Stripe.
- **Ícones e branding:** [`DESIGN_SYSTEM.md`](DESIGN_SYSTEM.md) — kit SVG (`assets/icons/`), `WhenoteIcons` / `WhenoteSvgIcon`, launcher (`assets/branding/app_icon.png`, `dart run flutter_launcher_icons`). [`README.md`](../README.md) / [`README.pt-BR.md`](../README.pt-BR.md) — tabela de stack e árvore do projeto com `assets/` e `shared/icons/`. [`ARCHITECTURE.md`](ARCHITECTURE.md) — entrada `shared/icons/`, parágrafo sobre assets na raiz. [`FLUTTER_COMMANDS.md`](../FLUTTER_COMMANDS.md) — comando para regenerar ícones do app.
- **Moderação por IA:** [`ARCHITECTURE.md`](ARCHITECTURE.md) — `core/admin`, `core/moderation`, `systemConfig/app`, coleções `moderationIncidents` e secção “Moderação de conteúdo”. [`functions/README.md`](../functions/README.md) — intro billing + moderation, env vars, Flutter (`ModerationFunctionsService`, `AdminFunctionsService`, região). [`MVP_CHECKLIST.md`](MVP_CHECKLIST.md) — item de moderação por IA marcado como entregue (base). [`ROADMAP.md`](ROADMAP.md) — linha “Moderação assistida (IA)” com referência à implementação base. [`README.md`](../README.md) / [`README.pt-BR.md`](../README.pt-BR.md) — Social + Backend com Cloud Functions e moderação opcional.
- **Cápsula coletiva:** [`ARCHITECTURE.md`](ARCHITECTURE.md) — campos `isCollective`, `participantUids`, `participantNames`, `contentMode`; subcoleção `contributions` (futuro); helpers e merge de queries no Cofre. [`MVP_CHECKLIST.md`](MVP_CHECKLIST.md) — modo grupo entregue; contribuições pré-selo como pós-MVP. [`ROADMAP.md`](ROADMAP.md) Fase 3 — linha de cápsula coletiva atualizada (MVP grupo vs. fase multi-contribuidor). [`README.md`](../README.md) / [`README.pt-BR.md`](../README.pt-BR.md) — tabela de cápsulas com opção coletiva.
- **Feed (arquitetura e custo):** [`ARCHITECTURE.md`](ARCHITECTURE.md) — secção “Feed” atualizada (Explorar / Destaques / Seguindo, chips fixados até 3 com `feed_pinned_filters_provider`, ícone de filtros à direita, `explore_feed_paged` / `following_feed_body`, merge `whereIn`). [`PRODUCTION.md`](PRODUCTION.md) — tabela Firestore com nota de custo do feed Seguindo e paginação em Explorar.
- **Roadmap e README:** [`ROADMAP.md`](ROADMAP.md) Fase 2 — badges e feed em 3 camadas como **Concluído** (alinhado a [`MVP_CHECKLIST.md`](MVP_CHECKLIST.md)); Fase 3 — linha de export renomeada para evoluções (MVP de export no checklist). [`README.md`](../README.md) / [`README.pt-BR.md`](../README.pt-BR.md) — tabelas “Social” e “Perfil” com camadas do feed, chips fixados e export. [`MVP_CHECKLIST.md`](MVP_CHECKLIST.md) — detalhe nos filtros do feed.
- **Produção:** [`PRODUCTION.md`](PRODUCTION.md) — checklist para build/lojas (`dart-define` `FB_APP_ID`, `BILLING_ENABLED`, `FUNCTIONS_REGION`), ficheiros Firebase e referências cruzadas.
- **Planejamento:** alinhamento de [`ROADMAP.md`](ROADMAP.md), [`CHANGELOG.md`](CHANGELOG.md) (release 0.9.0), [`README.md`](../README.md) / [`README.pt-BR.md`](../README.pt-BR.md), [`ARCHITECTURE.md`](ARCHITECTURE.md) e [`PRODUCTION.md`](PRODUCTION.md) com o estado em [`MVP_CHECKLIST.md`](MVP_CHECKLIST.md) (fonte de verdade para itens concluídos).
- **Email de autenticação:** [`EMAIL_SETUP.md`](EMAIL_SETUP.md) — guia completo de configuração SendGrid + SMTP + Firebase templates + página de ação customizada. [`PRODUCTION.md`](PRODUCTION.md) — secção "Email de autenticação (SMTP + templates)" com estado de configuração e DNS pendente. [`TROUBLESHOOTING.md`](TROUBLESHOOTING.md) — §9 "Emails de auth vão para spam ou confirmação sem branding".

### Added

- **Kit de ícones:** SVGs em [`assets/icons/`](../assets/icons/) (envelope, carta, cápsula, coração, cadeados, sino, brilhos, lacre, presente); [`lib/shared/icons/whenote_icons.dart`](../lib/shared/icons/whenote_icons.dart); dependência `flutter_svg`. Ícone do app: [`assets/branding/app_icon.png`](../assets/branding/app_icon.png) + **flutter_launcher_icons** (Android adaptativo fundo `#EDE5D8`, iOS AppIcon). Regenerar launchers: `dart run flutter_launcher_icons`.
- **Cápsula coletiva (grupo que abre junto):** na criação, opção “Só para mim” / **Coletiva** com convites por busca de utilizadores; Firestore: `participantUids`, `participantNames`, `isCollective`, `contentMode: singleAuthor`; regras em [`firestore.rules`](../firestore.rules) (leitura por participantes, `update` limitado para convidados, `delete` só criador); subcoleção `capsules/{id}/contributions/{uid}` reservada (leitura para participantes; escrita desligada até fase `multiContributor`). Cofre e badge do home: duas queries fundidas em [`capsule_vault_streams.dart`](../lib/features/capsules/data/capsule_vault_streams.dart). Detalhe: lista de participantes; eliminar só para o criador. Helper [`capsule_content_mode.dart`](../lib/shared/utils/capsule_content_mode.dart). Índice composto `participantUids` + `status` + `isCollective` em [`firestore.indexes.json`](../firestore.indexes.json).
- **Partilha Instagram Stories (nativo iOS/Android):** `MethodChannel` `com.whenote.app/instagram_stories` — iOS: pasteboard + `instagram-stories://share`; Android: `Intent` `com.instagram.share.ADD_TO_STORY` + `FileProvider` (`${applicationId}.fileprovider`). PNG 1080×1920 gerado em `StoryAssetBuilder` a partir de `StoryShareContent` (allowlist: título truncado, data, QR com deep link; **sem** corpo da carta nem Q&A). Fallback: `share_plus`. Configuração: `--dart-define=FB_APP_ID=…` ([`lib/core/config/facebook_app_config.dart`](../lib/core/config/facebook_app_config.dart)). UI: detalhe da carta, ecrã QR, cápsula (bottom sheet: Stories vs texto). Ver [`ARCHITECTURE.md`](ARCHITECTURE.md) (secção “Partilha social / Instagram Stories”).
- **Teclado — fechar global:** botão flutuante acima do teclado virtual quando este está aberto (`MediaQuery.viewInsets.bottom > 0`), registado no `MaterialApp.builder` (`lib/shared/widgets/keyboard_dismiss_overlay_button.dart`); toque remove o foco (`FocusManager`). Strings em ARB (en, pt, pt-BR, es). Ver [`ARCHITECTURE.md`](ARCHITECTURE.md) (secção “Overlays globais”).
- **Cofre — filtro e ordenação avançados:** ícone de ajustes no header abre bottom sheet com opções por aba (Aguardando, Abertas, Enviadas, Cápsulas): texto, ordenação, intervalo de data de abertura onde aplicável, recebidas/enviadas, só pendentes, temas de cápsula. Filtros aplicados **no cliente** sobre os resultados das queries Firestore existentes (`lib/features/letters/presentation/vault_list_filters.dart`, `widgets/vault_filter_sheet.dart`). Strings em ARB (pt-BR, en, es, pt). Ver `planning/ARCHITECTURE.md`.
- **Localização opcional (cartas e cápsulas):** ao gravar, diálogos perguntam se o remetente partilha a localização atual com o destinatário (`geolocator`; permissões Android/iOS/macOS) e, em seguida, se a abertura fica restrita a **≤ 10 m** desse ponto. Firestore: `senderLocation` (`lat`, `lng`, `capturedAt`), `openRequiresProximity`. Detalhe: tile escuro copia URL do Google Maps (`location_share_tile.dart`). Cofre: `openLetterWithProximityGate` / `openCapsuleWithProximityGate` antes dos ecrãs de abertura. Modelo `Letter` atualizado. A checagem de proximidade é **só no cliente** (ver `planning/ARCHITECTURE.md`).
- **Carta — mensagem recolhível e voz opcional:** na escrita, o campo “mensagem” digitada começa recolhido (`AnimatedSize`, pré-visualização de uma linha) e expande ao toque; gravação de voz opcional com limite de **1 minuto** (política Whenote), upload para Cloud Storage em `voiceLetters/` no envio (`record`, `permission_handler`, `path_provider`; web sem gravação — stub). Campo Firestore `voiceUrl`; reprodução com `just_audio` + `audio_session` (best-effort no `main`) em `letter_detail_screen` e `letter_opening_screen`. Validação: `voice_url.dart`; UI: `voice_letter_tile.dart`; upload: `voice_letter_io.dart` / `voice_letter_stub.dart`. Regras Storage: `handwritten/` e `voiceLetters/` em [`storage.rules`](../storage.rules).
- **Link de música (cartas e cápsulas):** campo opcional `musicUrl` no Firestore; o autor cola um URL `https` (ex.: Spotify, YouTube Music). Na abertura e no detalhe, o destinatário usa “Ouvir música” e o sistema abre o link no browser ou na app do serviço (`url_launcher`, `LaunchMode.externalApplication`). Validação: `lib/shared/utils/music_url.dart`; UI partilhada: `lib/shared/widgets/music_link_tile.dart`. Modelo `Letter` inclui `musicUrl`.
- **Selo animado (coruja)** na abertura da carta e no hero do login: olhos, revelação de asas/corpo, voo, lacre só em vermelho, reaparição do rosto completo **sem asas**; detalhe dos olhos (sobrancelha/cílios), tufts e penas; `OwlSealArt` / `OwlSealOpeningAnimation` em `lib/shared/widgets/owl_logo.dart`.
- **Feedback pela coruja nos headers:** `OwlFeedbackAffordance` envolve o glifo da coruja (`OwlWatermark` / `OwlLogo`); toque abre o mesmo bottom sheet que `FeedbackEntryButton` via `showFeedbackSheet`. Animação idle: oscilação + fase de “vibração” rápida, repetida em intervalos aleatórios por visita ao ecrã. FAB global de feedback permanece para utilizadores sem sessão (`main.dart`).

### Changed

- **Criação de cápsula — rótulo coletivo:** texto do botão do modo coletivo reduzido a uma palavra (“Coletiva” / “Colectiva” / “Collective”); explicação mantida em `createCapsuleCollectiveHint`.
- **Feed — preview de comentários:** no card (`feed_screen.dart`, `_buildCommentsPreview`), cada comentário longo fica limitado a **4 linhas** com reticências (como o corpo da carta). **Ler mais** (`feedReadMore`) aparece quando a heurística indica texto longo (> 120 caracteres ou ≥ 4 linhas) e expande só aquele comentário no preview (estado por ID Firestore `_expandedCommentPreviewIds`). O link **Ver todos os N comentários** e o limite 2 → 20 no preview mantêm-se. Ver [`ARCHITECTURE.md`](ARCHITECTURE.md) (secção “Feed”).
- **Planejamento:** Whenote Gift (Presente Selado / Gift When) e Nox Card descritos em [`ROADMAP.md`](ROADMAP.md), [`BUSINESS.md`](BUSINESS.md), [`MVP_CHECKLIST.md`](MVP_CHECKLIST.md) e nota em [`ARCHITECTURE.md`](ARCHITECTURE.md); conteúdo que estava em `planning/GIFT_FEATURE.md` foi integrado e esse arquivo removido. Política documentada no roadmap: novas ideias entram nas tabelas e no checklist, não em um `.md` por feature.

### Fixed

- **Admin — ecrã Moderação:** `TabBar` com `isScrollable: true` (rótulos completos, deslize horizontal nas abas); corpo dos cartões (denúncia, feedback, revisão humana, alertas IA) com `_BidirectionalScrollText` — texto seleccionável, quebra à largura do cartão e scroll horizontal para linhas muito longas (`admin_moderation_screen.dart`).
- **Onboarding:** overflow vertical em ecrãs curtos — `PageView` com `Positioned.fill` + página com scroll e altura mínima (`onboarding_screen.dart`).

---

## [0.9.0] - 2026-03-23

### Contexto

Release de referência histórica (março/2026). Na época, o MVP ainda tinha lacunas documentadas em “Known limitations” abaixo; **após** esta tag, os itens críticos foram fechados no código — ver estado atual em [`MVP_CHECKLIST.md`](MVP_CHECKLIST.md) (🔴 Crítico) e [`ROADMAP.md`](ROADMAP.md) Fase 1.

### Added (usuário)

- Onboarding, autenticação (login/cadastro), splash
- Feed com curtidas, comentários e filtros por emoção
- Cofre com abas: aguardando, abertas e **cápsulas**
- Escrever cartas, detalhe, leitura e animação de abertura
- Pedidos de carta, QR Code e compartilhamento
- Perfil (próprio e outros), busca por @username, configurações
- Telas legais (termos, privacidade, sobre, ajuda)
- **Cápsulas do tempo:** fluxo em 3 passos (Tema → Perguntas → Detalhes), persistência Firestore, listagem no cofre
- FAB com bottom sheet: nova carta ou nova cápsula
- Seguidores, bloqueios, moderação em comentários

### Added (projeto)

- Pasta `planning/` com roadmap, checklist de MVP, arquitetura, design system, negócio e changelog
- README voltado a desenvolvedores e investidores

### Known limitations / próximos passos *(no momento da release 0.9.0)*

Itens abaixo refletem o backlog **na data desta release**; não o estado atual do repositório.

- Tela dedicada de **abertura da cápsula** (revelação + publicar após revisão)
- **Avatar** com upload estável em web e mobile
- **FCM** end-to-end
- **Regras Firestore** de produção e testes em dispositivo real

**Atualização:** estes pontos foram subsequentemente concluídos e estão marcados no checklist (🔴). Próximo foco de produto: itens 🟡 Importante — ver [`MVP_CHECKLIST.md`](MVP_CHECKLIST.md) e [`ROADMAP.md`](ROADMAP.md) Fase 2.

[0.9.0]: https://github.com/Yuri-Lima/whenote/releases
